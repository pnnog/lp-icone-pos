import { render, screen } from '@testing-library/react';

import {{pascalCase name}} from '.';

describe('<{{pascalCase name}} />', () => {
  it('should render {{pascalCase}}', () => {
    render(<{{pascalCase name}} />);

    expect(
      screen.getByRole('heading', { name: /{{pascalCase name}}/i })
    ).toBeInTheDocument();
  });
});
